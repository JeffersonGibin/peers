{
  "name": "peers",
  "version": "1.0.0",
  "main": "memory-cache.js",
  "scripts": {
    "build": "tsc",
    "start": "node ./build/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "The project is made to test cache replicas. It uses the HTTP protocol to send data, and the cache is in memory.",
  "dependencies": {
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "typescript": "^5.7.2"
  }
}
